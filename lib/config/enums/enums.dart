enum AuthStates {
  Loading,
  Initial,
  Authenticated,
  Unauthenticated,
  Error,
}

enum PersonRole {
  buyer,
  seller,
}

enum PersonStatus {
  accepted,
  rejected,
  waiting
}